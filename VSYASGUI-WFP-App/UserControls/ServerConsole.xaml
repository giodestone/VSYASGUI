<UserControl x:Class="VSYASGUI_WFP_App.UserControls.ServerConsole"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VSYASGUI_WFP_App.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel>
            <!--<Label DockPanel.Dock="Top" Content="Server Console"/>-->
            <Grid DockPanel.Dock="Bottom" Margin="0 6 0 0">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Row="1" Grid.Column="0" x:Name="CommandTextBox"/>
                <Button Grid.Row="1" Grid.Column="1" x:Name="SendCommandButton" Content="Send Command" Margin="4 0 0 0"/>
            </Grid>

            <ScrollViewer>
                <TextBox x:Name="ConsoleTextBox"/>
            </ScrollViewer>
        </DockPanel>

        
    </Grid>
</UserControl>
